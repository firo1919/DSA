- `bisect_left(a, x)` → returns index of first element `>= x`.
    
- `bisect_right(a, x)` → returns index of first element `> x`.
    

```python
from bisect import bisect_left, bisect_right

arr = [1, 3, 3, 5, 6, 8, 17]

print(bisect_left(arr, 3))  # 1
print(bisect_right(arr, 3)) # 3
```
