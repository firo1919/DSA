## 1. Definition / Idea

Each prefix sum includes the current element.

## 2. Example (Python)

```python
def inclusive_prefix_sum(nums):
    prefix = []
    total = 0
    for num in nums:
        total += num
        prefix.append(total)
    return prefix
```
